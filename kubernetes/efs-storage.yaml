apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devops-project-efs
  namespace: devops-project
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: efs-sc
