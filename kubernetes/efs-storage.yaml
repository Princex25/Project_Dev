apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devops-project-efs
  namespace: devops-project
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: efs-sc